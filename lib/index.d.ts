import { upload, createDirectory } from './core/upload';
export { upload, createDirectory };
declare const _default: {
    upload: typeof upload;
    createDirectory: typeof createDirectory;
};
export default _default;
